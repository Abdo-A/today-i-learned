import PropTypes from 'prop-types';
import React from 'react';

const Navbar = (props) => {
  return (
    <div>
      <h1>Navbar</h1>
    </div>
  );
};

Navbar.propTypes = {};

export default Navbar;
